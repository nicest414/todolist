import 'package:flutter/material.dart';

class SugorokuTab extends StatelessWidget {
  const SugorokuTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(); // すごろくタブのウィジェット
  }
}
